FROM ubuntu:21.10

ENV VERSION 0.3.2

RUN ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime

RUN apt -y update && apt -y install curl && \
    curl -O https://raw.githubusercontent.com/vaxilu/x-ui/main/install.sh && chmod +x install.sh&& ./install.sh

CMD [ "sh", "-c", "/usr/local/x-ui/x-ui"]